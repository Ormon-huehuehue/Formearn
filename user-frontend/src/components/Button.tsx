import React from 'react'

const CustomButton = () => {
  return (
    <div className= "w-full bg-blue">
      <button className= "bg-red font-mono text-white text-lg"> Submit task</button>
    </div>
  )
}

export default CustomButton